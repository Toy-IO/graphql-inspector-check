type Query {
  product(id: ID!): Product!
  category(id: ID!): Category!
}

type Product {
  id: ID!
  name: String!
  description: String!
  price: Int!
}

type Category {
  id: ID!
  name: String!
  description: String!
  products: [Product!]!
}
